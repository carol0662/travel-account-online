{
  "name": "travel-account-online",
  "version": "1.2.0",
  "private": true,
  "description": "多人旅游记账 APP（Cloudflare Pages + D1 版，自带 *.pages.dev 永久免费域名）",
  "scripts": {
    "dev": "wrangler pages dev --binding DB=./.wrangler/d1 --persist"
  }
}
