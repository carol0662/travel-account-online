# 环境变量说明
# ------------------------------------------------------------------
# 本地开发（默认）：无需配置任何变量，自动使用 Node 内置 SQLite。
#
# 连接 Postgres（可选本地验证用）：
# DATABASE_URL=postgres://user:password@localhost:5432/travel
#
# 部署到 Render 时：DATABASE_URL 由 render.yaml 中的 fromDatabase 自动注入，
# 你不需要手动填写。只需保证 startCommand 能启动 server.js 即可。
#
# 服务端口（可选，默认 3000）：
PORT=3000
